#include<stdio>
#include<string>
using namespace std;

int main() {
string msg("HY000: UDF ERROR: Lower bound cannot be greater than or equal to the upper bound");
string err_str("UDF ERROR: Lower bound cannot be greater than or equal to the upper bound");

cout << error_string.compare(msg().size() - error_string.size(), error_string.size(), msg);


}
